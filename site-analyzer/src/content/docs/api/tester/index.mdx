---
title: API Tester
description: Documentación para el API Tester, una herramienta para probar diferentes escenarios de respuesta
---

# API Tester

El endpoint `/api/site/tester` permite probar diferentes escenarios de respuesta, simulando comportamientos específicos como errores, retrasos o respuestas parciales. Es una herramienta útil para verificar la integración de nuevos componentes y servicios.

> 🧪 **Herramienta de pruebas flexible** 
> 
> Esta API permite simular diferentes escenarios de respuesta para probar cómo tu aplicación
> maneja situaciones como errores, tiempos de espera o respuestas parciales. Ideal para pruebas
> de integración y desarrollo de componentes frontend.

## Características principales

- Respuestas en formato JSON estructurado
- Simulación de diferentes escenarios de respuesta (éxito, error, parcial, timeout)
- Configuración de retrasos para simular latencia
- Personalización de códigos y mensajes de error
- Soporte para diferentes tamaños de respuesta
- Inclusión de datos personalizados en la respuesta

import { UnifiedApiTester } from '@/components/ApiTester';

<UnifiedApiTester 
  apiType="tester" 
  title="API Tester"
  description="Prueba diferentes escenarios de respuesta con el API Tester."
  defaultEndpoint="/api/site/tester"
  defaultMethod="POST"
  defaultTestType="success"
  defaultDelay={0}
  defaultResponseSize="medium"
  showModelOptions={true}
  defaultModelType="anthropic"
  defaultModel="claude-3-5-sonnet-20240620"
  additionalFields={[
    {
      name: "testType",
      label: "Tipo de Prueba",
      type: "select",
      options: [
        { value: "success", label: "Éxito" },
        { value: "error", label: "Error" },
        { value: "partial", label: "Parcial" },
        { value: "timeout", label: "Timeout" }
      ],
      defaultValue: "success"
    },
    {
      name: "delay",
      label: "Retraso (ms)",
      type: "number",
      defaultValue: 0,
      min: 0,
      max: 10000
    },
    {
      name: "responseSize",
      label: "Tamaño de Respuesta",
      type: "select",
      options: [
        { value: "small", label: "Pequeño" },
        { value: "medium", label: "Mediano" },
        { value: "large", label: "Grande" }
      ],
      defaultValue: "medium"
    },
    {
      name: "simulateError",
      label: "Simular Error",
      type: "checkbox",
      defaultValue: false
    },
    {
      name: "errorCode",
      label: "Código de Error",
      type: "number",
      defaultValue: 500,
      min: 400,
      max: 599
    },
    {
      name: "errorMessage",
      label: "Mensaje de Error",
      type: "text",
      placeholder: "Error simulado para pruebas"
    }
  ]}
/>

## Parámetros de solicitud

| Parámetro | Tipo | Descripción | Valores posibles |
|-----------|------|-------------|------------------|
| `testType` | string | Tipo de prueba a realizar | `success`, `error`, `partial`, `timeout` |
| `delay` | number | Retraso en milisegundos | 0-10000 |
| `aiProvider` | string | Proveedor del modelo de IA | `openai`, `anthropic`, `gemini` |
| `aiModel` | string | ID del modelo de IA | Depende del proveedor |
| `customData` | object | Datos personalizados para incluir en la respuesta | Cualquier objeto JSON válido |
| `simulateError` | boolean | Si se debe simular un error | `true`, `false` |
| `errorCode` | number | Código de error HTTP | 400-599 |
| `errorMessage` | string | Mensaje de error personalizado | Cualquier texto |
| `responseSize` | string | Tamaño de la respuesta | `small`, `medium`, `large` |

## Ejemplos de uso

### Solicitud básica

```json
{
  "testType": "success",
  "responseSize": "small"
}
```

### Simular un error

```json
{
  "simulateError": true,
  "errorCode": 404,
  "errorMessage": "Recurso no encontrado"
}
```

### Simular un retraso

```json
{
  "testType": "timeout",
  "delay": 5000
}
```

### Incluir datos personalizados

```json
{
  "testType": "success",
  "customData": {
    "userId": "user_123",
    "preferences": {
      "theme": "dark",
      "notifications": true
    }
  }
}
```

## Respuesta

La respuesta incluye los siguientes campos:

| Campo | Tipo | Descripción |
|-------|------|-------------|
| `success` | boolean | Indica si la solicitud fue exitosa |
| `testType` | string | Tipo de prueba realizada |
| `timestamp` | string | Fecha y hora de la respuesta |
| `requestId` | string | ID único de la solicitud |
| `processingTime` | number | Tiempo de procesamiento en milisegundos |
| `aiProvider` | string | Proveedor del modelo de IA (si se especificó) |
| `aiModel` | string | ID del modelo de IA (si se especificó) |
| `data` | object | Datos generados según el tamaño solicitado |
| `customData` | object | Datos personalizados incluidos en la solicitud |
| `error` | object | Información del error (si ocurrió) |
| `metadata` | object | Metadatos adicionales |

## Códigos de estado

| Código | Descripción |
|--------|-------------|
| 200 | Solicitud exitosa |
| 400 | Parámetros inválidos |
| 500 | Error interno del servidor |
| Personalizado | Código de error especificado en la solicitud | 