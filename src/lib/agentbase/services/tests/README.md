# Test for agent_background

Please run this test to verify that the agent_background property is correctly passed from ProcessorInitializer to the processors when there is an agent_id.

To run the test:
```bash
npm test src/lib/agentbase/services/tests/AgentBackgroundFlow.test.ts
```
