---
title: leadQualificationManagement
description: Executes the leadQualificationWorkflow in Temporal for lead qualification management
---

import { MethodChip } from '../../components/MethodChip';

# leadQualificationManagement

Executes the leadQualificationWorkflow in Temporal for lead qualification management

<div className="endpoint-block">
  <MethodChip method="POST" />
  <code>/api/workflow/leadQualificationManagement</code>
</div>

## Body Parameters

| Parameter | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `site_id` | `string` | Yes | - |

## Example Request

```bash
curl -X POST "https://api.makinari.com/api/workflow/leadQualificationManagement" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "site_id": "value"
  }'
```
