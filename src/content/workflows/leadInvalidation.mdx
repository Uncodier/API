---
title: leadInvalidation
description: Ejecuta el workflow leadInvalidationWorkflow en Temporal para invalidar leads
---

import { MethodChip } from '../../components/MethodChip';

# leadInvalidation

Ejecuta el workflow leadInvalidationWorkflow en Temporal para invalidar leads

<div className="endpoint-block">
  <MethodChip method="POST" />
  <code>/api/workflow/leadInvalidation</code>
</div>

## Body Parameters

| Parameter | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `lead_id` | `string` | Yes | - |
| `site_id` | `string` | Yes | - |

## Example Request

```bash
curl -X POST "https://api.makinari.com/api/workflow/leadInvalidation" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "lead_id": "value",
    "site_id": "value"
  }'
```
