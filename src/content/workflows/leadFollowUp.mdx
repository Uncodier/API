---
title: leadFollowUp
description: Ejecuta el workflow leadFollowUpWorkflow en Temporal para seguimiento de leads
---

import { MethodChip } from '../../components/MethodChip';

# leadFollowUp

Ejecuta el workflow leadFollowUpWorkflow en Temporal para seguimiento de leads

<div className="endpoint-block">
  <MethodChip method="POST" />
  <code>/api/workflow/leadFollowUp</code>
</div>

## Body Parameters

| Parameter | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `site_id` | `string` | Yes | - |
| `lead_id` | `string` | Yes | - |

## Example Request

```bash
curl -X POST "https://api.makinari.com/api/workflow/leadFollowUp" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "site_id": "value",
    "lead_id": "value"
  }'
```
