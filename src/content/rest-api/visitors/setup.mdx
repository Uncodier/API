# Setup & Configuration

## Schema Initialization

*(Note: The legacy setup API endpoints (`/api/visitors/setup`, etc.) have been removed. The schema for `visitors`, `leads`, `visitor_sessions`, and `visitor_events` is now managed directly in the Supabase database.)*


