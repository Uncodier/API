---
title: buildSegments
description: Ejecuta el workflow buildSegmentsWorkflow en Temporal
---

import { MethodChip } from '../../../components/MethodChip';

# buildSegments

Ejecuta el workflow buildSegmentsWorkflow en Temporal

<div className="endpoint-block">
  <MethodChip method="POST" />
  <code>/api/workflow/buildSegments</code>
</div>

## Body Parameters

| Parameter | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `site_id` | `string` | Yes | - |

## Example Request

```bash
curl -X POST "https://api.makinari.com/api/workflow/buildSegments" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "site_id": "value"
  }'
```
