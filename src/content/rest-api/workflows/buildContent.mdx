---
title: buildContent
description: Ejecuta el workflow buildContentWorkflow en Temporal
---

import { MethodChip } from '../../../components/MethodChip';

# buildContent

Ejecuta el workflow buildContentWorkflow en Temporal

<div className="endpoint-block">
  <MethodChip method="POST" />
  <code>/api/workflow/buildContent</code>
</div>

## Body Parameters

| Parameter | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `site_id` | `string` | Yes | - |

## Example Request

```bash
curl -X POST "https://api.makinari.com/api/workflow/buildContent" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "site_id": "value"
  }'
```
