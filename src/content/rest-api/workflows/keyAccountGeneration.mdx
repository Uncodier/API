---
title: keyAccountGeneration
description: Ejecuta el workflow dailyStrategicAccountsWorkflow en Temporal para generación de cuentas estratégicas
---

import { MethodChip } from '../../../components/MethodChip';

# keyAccountGeneration

Ejecuta el workflow dailyStrategicAccountsWorkflow en Temporal para generación de cuentas estratégicas

<div className="endpoint-block">
  <MethodChip method="POST" />
  <code>/api/workflow/keyAccountGeneration</code>
</div>

## Body Parameters

| Parameter | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `site_id` | `string` | Yes | - |

## Example Request

```bash
curl -X POST "https://api.makinari.com/api/workflow/keyAccountGeneration" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "site_id": "value"
  }'
```
