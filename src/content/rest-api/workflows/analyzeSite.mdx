---
title: analyzeSite
description: Ejecuta el workflow analyzeSiteWorkflow en Temporal para análisis de sitio
---

import { MethodChip } from '../../../components/MethodChip';

# analyzeSite

Ejecuta el workflow analyzeSiteWorkflow en Temporal para análisis de sitio

<div className="endpoint-block">
  <MethodChip method="POST" />
  <code>/api/workflow/analyzeSite</code>
</div>

## Body Parameters

| Parameter | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `site_id` | `string` | Yes | - |

## Example Request

```bash
curl -X POST "https://api.makinari.com/api/workflow/analyzeSite" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "site_id": "value"
  }'
```
